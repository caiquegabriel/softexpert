# O arquivo "loja" que está nesta raiz é a base de dados. 
-> Fiz o teste restaurando a base com o arquivo "loja" e deu tudo certo. 


# O arquivo "conf.php" contém as variáveis para a conexão com a base de dados. 
-> Altere este arquivo com as credenciais do acesso do seu banco de dados Postgres. 
-> Na constante "ROOT" insira o endereço da raiz de onde se acessa o site/sistema, precisamos dele para carregar os estilos .css e scripts.js. 
-> Por exemplo, se o site estiver acessível em "http://localhost:8080", a variável "ROOT" deverá ser: 
--> const ROOT = "http://localhost:8080"

# Se houver algum erro ou conflito, entrar em contato 
---> cgabrieltd@gmail.com | Caíque Gabriel